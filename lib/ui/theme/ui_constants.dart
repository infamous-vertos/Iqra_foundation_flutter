import 'package:get/get.dart';

class UiConstants{
  final screenWidth = Get.context!.isPortrait ? 360.0 : 690.0;
  final screenHeight = Get.context!.isPortrait ? 690.0 : 360.0;

  static const rupeeSymbol = "₹";
}